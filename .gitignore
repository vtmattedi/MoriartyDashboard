.pio
.vscode/.browse.c_cpp.db*
.vscode/c_cpp_properties.json
.vscode/launch.json
.vscode/ipch

# Secrets. creds.h carries the WiFi password, the MQTT broker password and the
# broker's root CA -- it must never be committed. include/creds.example.h is the
# template to copy; keep it in sync when adding a define.
/include/creds.h
/include/Creds

# Generated by src/Version/script.py on every build.
/include/Version.h

# --- HybridCompile leftovers -------------------------------------------------
# Generated by a `custom_sdkconfig` build (see platformio.ini), which is
# disabled and does not work on this host. None of it is source: managed_components
# alone is ~175MB of vendored ESP-IDF components fetched by the component manager.
/managed_components
/dependencies.lock
/sdkconfig.defaults
/sdkconfig.*
/CMakeLists.txt
/.dummy

# Local agent configuration, not project source.
/.claude
